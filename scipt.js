function downloadVersion(version) {
    alert(`You clicked to download version ${version}.`);
    // Here you can add the actual download logic or redirect to the download link
    // For example:
    // window.location.href = `downloads/minecraft-client-${version}.zip`;
}
